//write a function that takes a string representing a roman numerals and returns its value as an integer.
// example input: "XXII"
// expected output: 22

// you can safely assume that all inputs will be valid roman numerals

export function romanNumeralsTranslator(romanNumeral) {
  return
}
